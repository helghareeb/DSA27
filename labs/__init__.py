"""DSA27 lab exercises — weeks 1 to 3.

These are the Python warm-up exercises that go with the lab manual in
`docs/labs/`. Each file is one lab session:

    labs/lab01.py   Week 1 — the interpreter, numbers, strings, lists
    labs/lab02.py   Week 2 — control flow, functions, errors, modules
    labs/lab03.py   Week 3 — data structures, classes, generators

Every function is a skeleton that raises `NotImplementedError`. The tests in
`tests/test_lab01.py`, `tests/test_lab02.py` and `tests/test_lab03.py` define
what "correct" means:

    pytest tests/test_lab01.py -v      # one lab
    pytest -m lab                      # all three labs
"""
